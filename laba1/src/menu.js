import { BrowserWindow } from "electron";
// import { METHODS } from "http";
import {createNewWindow} from './index.js';

const columnBtn = document.getElementById("column-method");

columnBtn.addEventListener('click', () => alert('while runnin a mile'));
// alert('workin');
/*
const menuWindow = new BrowserWindow({
    width: 1000,
    height: 1000,
})
*/